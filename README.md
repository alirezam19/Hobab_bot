# 🤖 ربات تلگرام تحلیل‌گر بازار طلا، ارز و رمزارز (Hobabbot) 🚀

![Python Version](https://img.shields.io/badge/Python-3.11+-blue?style=for-the-badge&logo=python)
![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)
![PRs Welcome](https://img.shields.io/badge/PRs-welcome-brightgreen.svg?style=for-the-badge)

یک ربات تلگرامی پیشرفته، کاملاً شخصی‌سازی‌شده و پایدار برای دریافت قیمت‌های لحظه‌ای، تحلیل حباب سکه و طلا، و دریافت گزارش‌های خودکار زمان‌بندی‌شده. این ربات با معماری بهینه و با تمرکز بر تجربه کاربری (UX) برای زبان فارسی طراحی شده است.

**(✨ یک GIF جذاب از عملکرد ربات در اینجا قرار دهید ✨)**
`![Bot Demo](link/to/your/demo.gif)`

---

## 🌟 ویژگی‌های کلیدی

*   **قیمت‌های لحظه‌ای:** دریافت آنی قیمت طلا، انواع سکه، ارزهای رایج و رمزارزهای محبوب از API.
*   **داشبورد تحلیل حباب:**
    *   محاسبه دقیق حباب برای ۶ آیتم کلیدی: سکه امامی، بهار آزادی، نیم، ربع، طلای ۱۸ عیار و مثقال.
    *   ارائه تحلیل استراتژیک بر اساس درصد حباب سکه امامی.
    *   نمایش تاریخ و ساعت شمسی در هدر تمام گزارش‌ها.
*   **شخصی‌سازی پیشرفته:**
    *   منوی تنظیمات اختصاصی برای هر کاربر.
    *   قابلیت انتخاب نمایش یا عدم نمایش هر آیتم در لیست قیمت‌ها.
    *   ذخیره تنظیمات هر کاربر به صورت مجزا در پایگاه داده JSON.
*   **زمان‌بندی خودکار و چندگانه:**
    *   قابلیت تنظیم دریافت گزارش‌های تجمیعی در **چندین ساعت مختلف** از شبانه‌روز.
    *   انتخاب نوع گزارش‌های خودکار (ارز، طلا، حباب و...).
    *   ارسال گزارش‌های خودکار کاملاً شخصی‌سازی‌شده بر اساس آیتم‌های انتخابی کاربر.
*   **رابط کاربری زیبا و بهینه:**
    *   استفاده ترکیبی از دکمه‌های سخت (منوی اصلی) و دکمه‌های شیشه‌ای (تنظیمات).
    *   نمایش اموجی‌های مرتبط و زیبا برای تمام آیتم‌ها.
    *   پشتیبانی کامل از زبان فارسی و **حل مشکل راست‌چین (RTL)** در نمایش اموجی‌ها.
*   **معماری حرفه‌ای:**
    *   استفاده از فایل `.env` برای مدیریت متغیرهای محیطی و کلیدهای API.
    *   کدنویسی تمیز و ماژولار با کامنت‌گذاری کامل.
    *   آماده استقرار روی سرور لینوکس به عنوان یک سرویس دائمی با `systemd`.

---


---

## 🛠️ راه‌اندازی و نصب (محیط توسعه)

برای اجرای این ربات روی سیستم شخصی خود، مراحل زیر را دنبال کنید.

1.  **کلون کردن ریپازیتوری:**
    ```bash
    git clone https://github.com/your-username/hobabbot.git
    cd hobabbot
    ```

2.  **ساخت فایل `.env`:**
    یک فایل با نام `.env` در ریشه پروژه بسازید و کلیدهای API خود را در آن قرار دهید:
    ```env
    TELEGRAM_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
    BRSAPI_KEY=YOUR_BRSAPI_API_KEY
    ```

3.  **ساخت و فعال‌سازی محیط مجازی (Virtual Environment):**
    ```bash
    # ساخت محیط مجازی
    python3 -m venv venv

    # فعال‌سازی در ویندوز
    .\venv\Scripts\activate

    # فعال‌سازی در لینوکس/مک
    source venv/bin/activate
    ```

4.  **نصب نیازمندی‌ها:**
    فایل `requirements.txt` تمام کتابخانه‌های لازم را به صورت خودکار نصب می‌کند.
    ```bash
    pip install -r requirements.txt
    ```

5.  **اجرای ربات:**
    ```bash
    python3 bot.py
    ```
    ربات شما اکنون در حال اجراست و به پیام‌ها پاسخ می‌دهد.

---

## 🐧 اجرا به عنوان سرویس دائمی در سرور لینوکس (با `systemd`)

برای اینکه ربات به صورت ۲۴/۷ و حتی پس از ریبوت سرور فعال بماند، مراحل زیر را در سرور لینوکسی خود (پس از انجام مراحل نصب بالا) دنبال کنید.

1.  **ساخت فایل سرویس:**
    ```bash
    sudo nano /etc/systemd/system/hobabbot.service
    ```

2.  محتوای زیر را در فایل کپی کنید. **(حتماً `User` و مسیرها را با مقادیر سرور خود جایگزین کنید)**:
    ```ini
    [Unit]
    Description=Telegram Bot for Gold Prices (Hobabbot)
    After=network.target

    [Service]
    User=root
    Group=root
    WorkingDirectory=/root/hobabbot
    ExecStart=/root/hobabbot/venv/bin/python3 bot.py
    Restart=always
    RestartSec=10

    [Install]
    WantedBy=multi-user.target
    ```

3.  **مدیریت سرویس:**
    ```bash
    # بارگذاری سرویس جدید
    sudo systemctl daemon-reload

    # اجرای فوری ربات
    sudo systemctl start hobabbot

    # فعال‌سازی برای اجرای خودکار در بوت
    sudo systemctl enable hobabbot

    # بررسی وضعیت ربات
    sudo systemctl status hobabbot
    ```

---

## 📚 کتابخانه‌های اصلی استفاده شده

*   **[python-telegram-bot](https://python-telegram-bot.org/)**: برای تعامل با Telegram Bot API.
*   **[python-dotenv](https://pypi.org/project/python-dotenv/)**: برای مدیریت متغیرهای محیطی.
*   **[requests](https://pypi.org/project/requests/)**: برای ارسال درخواست‌های HTTP به API قیمت.
*   **[jdatetime](https://pypi.org/project/jdatetime/)**: برای کار با تاریخ و زمان شمسی.
*   **[pytz](https://pypi.org/project/pytz/)**: برای مدیریت دقیق مناطق زمانی (Timezones).

---

## 🤝 مشارکت (Contributing)

از هرگونه مشارکت، گزارش باگ یا پیشنهاد برای بهبود این پروژه به گرمی استقبال می‌شود. لطفاً یک Pull Request یا Issue جدید ثبت کنید.

--
